[user]
	name = 
	email = 
	signingkey = 

[diff]
	tool = meld
	guitool = vimdiff

[merge]
	tool = meld

[alias]
	co = checkout
	ci = commit
	st = status
	br = branch
	hist = log --pretty=format:\"%ad %C(yellow bold)%h%C(reset) %s%C(red bold)%d%C(reset) %C(cyan bold)[%an]%C(reset)\" --graph --date=short
